import { colors, textSecondary } from './styles'

const spacing = (number: number) => number * 8
const borderRadius = (number: number) => number * 8
const fontSize = (number: number) => number * 8

export const theme = {
	borderRadius,
	spacing,
	colors,
	fontSize,
	textSecondary,
}
